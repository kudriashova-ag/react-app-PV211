import React from 'react';

const TodoFilter = () => {
    return (
      <div className="filter">
        <button>Todo</button>
        <button>Done</button>
        <button>All</button>
      </div>
    );
}

export default TodoFilter;
